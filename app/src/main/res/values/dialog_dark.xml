<?xml version="1.0" encoding="utf-8"?>
    <resources xmlns:tools="http://schemas.android.com/tools">
        <!-- Base application theme. -->
    <style name="Theme.OnlinePlaylistsDialog" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:colorBackground">@color/grey3</item>
        <item name="android:textColor">@color/grey1</item>
        <item name="android:textColorPrimary">@color/grey1</item>
    </style>
    </resources>