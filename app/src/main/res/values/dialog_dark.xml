<?xml version="1.0" encoding="utf-8"?>
    <resources xmlns:tools="http://schemas.android.com/tools">
        <!-- Base application theme. -->
    <style name="Theme.OnlinePlaylistsDialog" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:colorBackground">@color/grey3</item>
        <item name="android:colorControlActivated">@color/soft_red</item>
    </style>
    </resources>